name: 'finance_data_platform'
version: '1.0.0'

profile: 'finance_data_platform'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

vars:
  firds_base_date: '2026-07-18'

models:
  finance_data_platform:
    marts:
      +materialized: table


# models:
#   calendar:
#     +materialized: view
